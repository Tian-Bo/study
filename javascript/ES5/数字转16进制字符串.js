
        let color = Math.floor(Math.random() * 256).toString(16);
        console.log(color.length)
        console.log(color)
        let abb = {
            name: 1,
            age: '男'
        }
